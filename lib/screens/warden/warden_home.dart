import 'package:flutter/material.dart';

class WardenHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Warden Dashboard')),
      body: Center(
        child: Text('Warden View - Work in Progress'),
      ),
    );
  }
}